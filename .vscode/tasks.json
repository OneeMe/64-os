{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "cd ${workspaceFolder}/output && nasm -fmacho64 ${file} -o ${fileBasenameNoExtension}.o && ld -macosx_version_min 10.7.0 ${fileBasenameNoExtension}.o",
            "problemMatcher": []
        }
    ]
}
